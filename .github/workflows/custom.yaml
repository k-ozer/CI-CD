name: CoderCo Custom Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: hello world custom action
      uses: k-ozer/coderco-custom@main
      with:
          who-to-greet: 'CoderCo Team'